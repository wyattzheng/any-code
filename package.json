{
  "name": "any-code",
  "private": true,
  "scripts": {
    "dev:app": "pnpm --filter app dev",
    "build:agent": "pnpm --filter agent build",
    "build:server": "pnpm --filter server build",
    "build": "pnpm -r build"
  }
}
