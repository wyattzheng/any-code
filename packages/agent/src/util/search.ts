/**
 * SearchProvider — re-exported from @any-code/utils.
 *
 * The canonical definitions live in @any-code/utils to avoid
 * circular dependencies between agent and utils.
 */
export type { SearchProvider, GrepMatch } from "@any-code/utils"
