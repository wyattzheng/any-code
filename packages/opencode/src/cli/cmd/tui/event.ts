/**
 * Stub for TUI event types
 * The CLI TUI module was not forked — this provides minimal stubs
 * so that MCP and other modules can reference TuiEvent without error.
 */
export namespace TuiEvent {
    export const ToastShow = "tui.toast.show"
    export const ToastDismiss = "tui.toast.dismiss"
}
